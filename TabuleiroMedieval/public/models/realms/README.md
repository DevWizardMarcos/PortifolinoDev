# /models/realms/

Coloque aqui, um GLB por reino (se um dia os castelos procedurais forem
substituídos por modelos reais):

- `infinity.glb`
- `cnx.glb`
- `dreams.glb`
- `ms.glb`
- `danki.glb`

Nenhum componente carrega esses arquivos ainda — os castelos continuam
gerados proceduralmente em `src/world/Markers.js`.
